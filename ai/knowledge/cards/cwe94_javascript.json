{
  "cwe": "CWE-94",
  "name": "CODE_INJECTION",
  "language": "javascript",
  "functional_semantics": "Dữ liệu người dùng được đưa vào bộ thông dịch JavaScript để thực thi",
  "sources": ["req.query", "req.body", "req.params", "req.headers", "req.cookies"],
  "sinks": ["eval", "Function(", "new Function", "vm.runInNewContext", "vm.runInThisContext", "setTimeout với chuỗi", "setInterval với chuỗi"],
  "sanitizers": [
    "JSON.parse() thay cho eval",
    "Allow-list ánh xạ input sang hàm cố định",
    "Kiểm tra regex chặt trước khi đưa vào Function",
    "vm với sandbox giới hạn và timeout (giảm nhẹ, không triệt để)",
    "Number()/parseInt() cho biểu thức số"
  ],
  "root_cause": "eval và new Function biên dịch chuỗi thành mã nên input điều khiển được luồng thực thi",
  "fp_indicators": [
    "Dữ liệu được parse bằng JSON.parse chứ không phải eval",
    "Hàm gọi lấy từ bảng ánh xạ cố định thay vì dựng từ chuỗi",
    "Có kiểm tra regex chỉ cho ký tự an toàn ngay trước sink",
    "Chuỗi đưa vào Function là hằng số trong mã",
    "setTimeout nhận callback function chứ không phải chuỗi"
  ],
  "few_shot": [
    {
      "code": "const q = req.query.expr;\nres.send(String(eval(q)));",
      "label": "TP",
      "why": "eval trên chuỗi từ query string"
    },
    {
      "code": "const op = req.query.op;\nconst ops = { add: (a,b)=>a+b, sub: (a,b)=>a-b };\nif (!(op in ops)) return res.sendStatus(400);\nres.send(String(ops[op](1,2)));",
      "label": "FP",
      "why": "Chỉ chọn được hàm trong bảng cố định, không dựng mã từ chuỗi"
    }
  ]
}
