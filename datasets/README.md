# Datasets

This directory is for research datasets used by Aegis-SAST.

## Planned layout

```text
datasets/
  synthetic/
  generated/
```

## Rules

- `synthetic/` is for hand-crafted and labeled samples
- `generated/` is for machine-generated or exported data and should remain ignored
- store metadata, labels, and provenance notes with every dataset
